# EasyDo 仓库协作规范

## 代码质量

- 保持代码结构清晰, 类型严格, 职责单一.
- 提交前运行格式检查, 静态检查, 类型检查和构建检查.
- 代码注释使用中文, 仅在解释设计原因或复杂逻辑时添加注释.
- 中文文本中的标点符号统一使用英文标点符号.
- 不在基础设施变更中擅自加入业务功能.

## Commit message

- 格式为 `type: 简洁中文说明.`.
- 使用简洁中文描述改动, 并以英文句号结尾.
- 前缀可使用 `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`, `revert`.
- 禁止使用中文标点符号.
- 示例为 `feat: 初始化项目结构.`.

## Git 文件管理

- 创建项目源文件后及时加入 Git.
- 生成文件和本地文件应加入 `.gitignore`.
- 交付前检查 `git status`, 不留下预期之外的未跟踪文件.
