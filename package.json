{
  "name": "easydo",
  "version": "1.10.0",
  "private": true,
  "description": "Local-first task management and calendar application.",
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "pnpm --filter @easydo/web dev",
    "desktop:build": "pnpm --filter @easydo/desktop build",
    "desktop:build:universal": "pnpm --filter @easydo/desktop build:universal",
    "desktop:check": "pnpm --filter @easydo/desktop check",
    "desktop:dev": "pnpm --filter @easydo/desktop dev",
    "build": "pnpm --filter @easydo/web build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings 0",
    "prepare": "node scripts/configure-git-hooks.mjs",
    "test": "pnpm --filter @easydo/web test",
    "test:coverage": "pnpm --filter @easydo/web test:coverage",
    "test:e2e": "pnpm --filter @easydo/web test:e2e",
    "typecheck": "pnpm --recursive --if-present typecheck"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.9.1",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.5",
    "globals": "17.11.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.68.0"
  }
}
